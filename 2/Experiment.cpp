#include "ExDispid.h"

#include <stdlib.h>
#include <math.h>
#include <time.h>

int experiment (double* radioactivity, double* time, double start_time, double step) {
    
}

void add_noise (double *rediactivity, int N) {
    srand(time(NULL));
}